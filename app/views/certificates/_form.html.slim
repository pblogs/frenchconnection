= form_for @certificate do |f|
  - if @certificate.errors.any?
    #error_explanation
      h2 = "#{pluralize(@certificate.errors.count, "feil")} forhindret oss i å lagre:"
      ul
        - @certificate.errors.full_messages.each do |message|
          li = message

  .field
    = f.label :title
    = f.text_field :title
  .actions = f.submit t('save')
