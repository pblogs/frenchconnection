section#todoapp[ng-controller="MainCtrl"]
  ng-view
  header#header
    h1
      | tasks
    form#todo-form[ng-submit="save(activeTodo)"]
      input#new-todo[placeholder="What needs to be done?" ng-model="activeTodo.title" autofocus]
  section#main[ng-show="tasks.length" ng-cloak]
    input#toggle-all[type="checkbox" ng-model="checked" ng-click="checkAll(checked)"]
    label[for="toggle-all"]
      | Mark all as complete
    ul#todo-list
      li[ng-repeat="task in tasks " ng-class="{completed: task.completed, editing: task == editedTodo}"]
        .view
          input.toggle[type="checkbox" ng-model="task.completed" ng-click="checkAndSave(task)"]
          label[ng-dblclick="edit(task)"]
            | {{task.description}}
          button.destroy[ng-click="remove(task)"]
  footer#footer[ng-show="tasks.length" ng-cloak]
    span#task-count
      strong
        | {{remainingCount}}
      ng-pluralize[count="remainingCount" when="{ one: 'item left', other: 'items left' }"]

    ul#filters
      li
        a[ng-class="{selected: status == ''} " href="#/"]
          | All
      li
        a[ng-class="{selected: status == 'active'}" href="#/active"]
          | Active
      li
        a[ng-class="{selected: status == 'completed'}" href="#/completed"]
          | Completed
    button#clear-completed[ng-click="clearCompletedTodos()" ng-show="completedCount"]
      | Clear completed ({{completedCount}})
  footer#info
    p
      | Double-click to edit a task
    p
      | Credits: 
      a[href="http://twitter.com/brettcassette"]
        | Brett Cassette
      | , 
    p
      | Part of 
      a[href="http://todomvc.com"]
        | TodoMVC
