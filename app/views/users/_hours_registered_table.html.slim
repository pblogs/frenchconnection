- if hours

  table#hours_registered
    tr
      th Dato
      th Tekst
      th Timer
      th 50%
      th 100%
      th Servicebil
      th Km egen bil
      th Bomutg.
      th Materiell
      th 
    - hours.each do |hour|
      tr
        td= hour.date.strftime("%d/%m")
        td= hour.description
        td= hour.hour
        td= hour.overtime_50
        td= hour.overtime_100
        td= hour.runs_in_company_car
        td= hour.km_driven_own_car
        td= hour.toll_expenses_own_car
        td= simple_format hour.supplies_from_warehouse
        td
          = link_to fa_icon('pencil'), edit_task_hours_spent_path(hour.task, hour)
- else
  br
  br
  p Det er ikke registert noen timer på dette prosjektet.
  br
  br
